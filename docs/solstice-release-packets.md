# Solstice release packets

Solstice assembles the release packet through a package-compatible region
adapter. The adapter accepts the current packet region and selects an allowed
canonical release region from the packet policy.
