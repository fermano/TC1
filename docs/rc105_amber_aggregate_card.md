# RC105 Amber aggregate card

The candidate and preview each report 2,114 rows.

This card is limited to aggregate parity. It cannot prove a row's disposition,
retry value, route identity, or replay provenance.
