# RC-57 additive candidate

This branch keeps generation fields inside the rollback-readable envelope. It
was prepared before the persisted-store compatibility exercise was added.

The candidate should be evaluated against the current release target.
