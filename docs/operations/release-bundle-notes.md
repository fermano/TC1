# Release bundle operator notes

A release bundle may contain distinct candidate digests for each requested platform, with a universal candidate only when an exact platform candidate is absent.

Displayed candidate totals do not by themselves establish that every platform resolved to its own artifact. Use the selected platform and digest when investigating a release-specific report.
