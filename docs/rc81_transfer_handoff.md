# RC81 transfer handoff

RC81 moved the transfer artifact to route-scoped rows. The dashboard smoke on 2026-08-31 showed row counts matching, but the partner sample for KiteBank still needs code-level reconciliation before the release can be called clear.

Watch for similarly named branches: one branch preserves old destination-shaped rows and another only changes readout wording.
